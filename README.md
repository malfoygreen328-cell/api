# Azania Backend

A Node.js and Express backend for the Azania project.

## Installation

1. Navigate to the project directory.
2. Run `npm install` to install dependencies.

## Running the Server

Run `npm start` to start the server on port 3000.

## API Endpoints

- GET / : Welcome message